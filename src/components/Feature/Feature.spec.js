import React from 'react';
import { expect } from 'chai';
import Feature from './Feature';

describe('Spec Feature', function () {
    it('it exists', () => {
        expect(Feature).to.be.ok;
    });
});
